---
import { Image } from "astro:assets";
import { getData } from "../utils/projects";
const data = getData();
---

<section id="projects" class="text-primary body-font w-full md:w-11/12 lg:w-5/6 mx-auto px-4 sm:px-6 md:px-0">
  <div class="py-16 sm:py-20 md:py-24 mx-auto">
    <h1 data-aos="fade-up" data-aos-anchor-placement="center-bottom" class="mb-6 sm:mb-8 font-extrabold text-center text-3xl sm:text-4xl text-transparent bg-clip-text bg-gradient-to-tr from-[#d7d7d7] to-[#616161]">Proyectos recientes</h1>

    <p data-aos="fade-up" data-aos-anchor-placement="center-bottom" class="text-gray-400 font-medium text-base sm:text-lg text-center mb-12 sm:mb-16 w-full sm:w-11/12 lg:w-4/5 mx-auto">
      Explora mis proyectos y descubre la amplia variedad de sitios web que he creado y desarrollado. Cada proyecto cuenta con una descripción detallada y enlaces para que puedas explorar más a fondo. <br class="hidden sm:block" /><br class="hidden sm:block" />¡Déjate sorprender por lo que puedo hacer por ti!
    </p>

    <div class="flex flex-col sm:flex-row items-center sm:items-end sm:justify-between py-6 sm:py-8 gap-4 sm:gap-0">
      <h1 class="text-primary text-xl sm:text-2xl md:text-3xl font-semibold">Todos los Proyectos</h1>
    </div>

    <div class="mx-auto grid grid-cols-1 gap-6 sm:gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {
        data &&
          data.map((item, index) => {
            return (
              <a data-aos="fade-up" data-aos-anchor-placement="center-bottom" href={`/projects/${item.slug}`} class="relative mx-auto overflow-hidden rounded-xl bg-transparent border border-1 border-primary border-opacity-25 shadow-xl group transition-all duration-300 hover:shadow-2xl hover:scale-[1.02] w-full">
                <div class="bg-transparent relative overflow-hidden h-48 sm:h-56 md:h-64 w-full">
                  <Image src={item.image} class="object-cover w-full h-full bg-cover rounded-t-xl transition-transform duration-500 group-hover:scale-105" alt={`Imagen del proyecto ${item.name}`} transition:name={item.name} />
                  <div class="absolute inset-0 bg-gradient-to-b from-transparent to-black/70 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end">
                    <div class="p-4 text-white transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
                      <p class="font-medium text-sm mb-2">{item.description ? item.description.substring(0, 120) + "..." : "Ver detalles del proyecto"}</p>
                    </div>
                  </div>
                </div>
                <div class="py-4 px-4 rounded-b-xl bg-opacity-75 h-auto">
                  <div class="flex items-center justify-between">
                    <div>
                      <h1 class="text-lg sm:text-xl font-bold text-white">{item.name}</h1>
                      <p class="mb-2 text-xs sm:text-sm text-white text-opacity-80">{item.type}</p>
                      {item.tech && (
                        <div class="flex flex-wrap gap-1 mt-1">
                          {item.tech.slice(0, 3).map((tech) => (
                            <span class="inline-block text-xs py-1 px-2 rounded-full bg-primary bg-opacity-20 text-primary/80">{tech}</span>
                          ))}
                          {item.tech.length > 3 && <span class="inline-block text-xs py-1 px-2 rounded-full bg-primary bg-opacity-10 text-primary/80">+{item.tech.length - 3}</span>}
                        </div>
                      )}
                    </div>
                    <button aria-label={`Ver proyecto ${item.name}`} class="p-2 mr-2 bg-primary rounded-full bg-opacity-10 transition-all duration-300 hover:bg-opacity-30 transform hover:scale-110">
                      <svg class="fill-primary h-6" viewBox="0 0 24 24" height="1em" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round" />
                        <g>
                          <g id="SVGRepo_bgCarrier" stroke-width="0" />
                          <g id="SVGRepo_iconCarrier">
                            <path
                              fill-rule="evenodd"
                              clip-rule="evenodd"
                              d="M12.2929 4.29289C12.6834 3.90237 13.3166 3.90237 13.7071 4.29289L20.7071 11.2929C21.0976 11.6834 21.0976 12.3166 20.7071 12.7071L13.7071 19.7071C13.3166 20.0976 12.6834 20.0976 12.2929 19.7071C11.9024 19.3166 11.9024 18.6834 12.2929 18.2929L17.5858 13H4C3.44772 13 3 12.5523 3 12C3 11.4477 3.44772 11 4 11H17.5858L12.2929 5.70711C11.9024 5.31658 11.9024 4.68342 12.2929 4.29289Z"
                            />
                          </g>
                        </g>
                      </svg>
                    </button>
                  </div>
                </div>
              </a>
            );
          })
      }
    </div>
  </div>
</section>
